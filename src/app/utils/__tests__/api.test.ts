import { getNewApplicant } from '../api';

describe('api', () => {
    describe('getNewApplicant', () => {
        it('true should be true', () => {
            expect(true).toEqual(true);
        });
    });
});
